import { SettingsScreen } from "@/components/console/settings/SettingsScreen";

export const metadata = {
  title: "Settings · Relay",
  description:
    "Your Relay profile, your connection to the network, and how to reach support.",
};

export default function SettingsPage() {
  return <SettingsScreen />;
}
